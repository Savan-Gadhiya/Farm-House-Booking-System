import { Container, Text } from '@chakra-ui/react';
import React from 'react';

const VerificationRequests = () => {
  return <>
		<Container m={3} p={3}>
			<Text as={'h1'}> Verification Requests </Text>
		</Container>
	</>;
};

export default VerificationRequests;
